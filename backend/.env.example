DATABASE_URL=mysql://roadmap:roadmap@localhost:3306/roadmap
SECRET_KEY=change-me-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440
FRONTEND_URL=http://localhost:3000

# AI feature extraction (Anthropic). Leave blank to disable AI drafting (endpoint
# returns 503). In prod, set this as a Railway environment variable — never commit it.
ANTHROPIC_API_KEY=
AI_MODEL=claude-sonnet-4-6
